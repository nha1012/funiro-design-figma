import { Settings } from "react-slick";

export const settingsCarousel: Settings = {
  className: "carousel-wrapper",
  infinite: false,
  centerPadding: "20px",
  slidesToShow: 2,
  swipeToSlide: true,
  variableWidth: true,
};
